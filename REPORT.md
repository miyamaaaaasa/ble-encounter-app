# REPORT — Sprint A「出会った人を魅力的に見せる」（beta1.7.7+40）

作業日: 2026-07-07 / 検証: 実機2台（A202SH・Pixel）＋ Pixel 10 Emulator

---

## ☕ 3分要約

1. **ドット絵同期が完成** — 「じぶんアイコン＝マイピース」に統一することで、
   **SQL作業ゼロ**で相手のドット絵が Today/広場/名簿/開封カード/詳細に表示される
   ようになりました（既存の users.piece_data 同期路線を再利用）
2. **全画面の相手表示を PeerIcon に統一** — 優先順位「①相手のドット絵 ②画像
   ③イニシャル」。ドット絵はにじまない角丸タイルで主役化
3. **過去の出会いにも遡及** — カケラに保存済みの絵を名簿へ自動補完する
   一回きりのマイグレーションを実装
4. **バグ1件を仕様化前に検出** — Bluetooth OFF だと同期自体が走らない問題を発見し、
   同期をBLE起動から独立させた
5. テスト37件全パス・実機スモークOK・A202SHの実画面で新UI動作確認済み

---

## 🎨 ドット絵同期の実装内容（保存→同期→復元→キャッシュ→表示）

| 段階 | 実装 |
|---|---|
| 保存 | 従来どおり `DotAvatarStorage`（端末内・再起動復元） |
| 同期↑ | エディタ保存時に即時 `savePieceData()` ＋ 起動時 `_syncProfileToServer()`（**BLE起動と独立**・BT OFFでも送信）。32x32は `toPiecePixels()` で16x16へ間引き |
| 同期↓ | 既存 `resolve_tokens` の piece_data をそのまま利用（サーバー変更なし） |
| キャッシュ | `EncounterRecord.peerPixels`（256画素・SharedPreferences永続化。`px`キー） |
| 復元 | 過去データ向けに「カケラ→名簿」自動補完マイグレーション（起動時1回） |
| 表示 | 新共通コンポーネント **PeerIcon**（ドット絵＞画像＞イニシャル、flipX対応） |

## 🌞 Today改善

- 円弧カルーセルの人物を**白台座の角丸ドット絵タイル(96px)**に刷新（絵が主役）
- 開封カードの主役も**ドット絵116px白台座**に
- ひとこと吹き出し・祝福ポップ・ぷるぷる待機・曖昧時刻表現は維持
- 履歴タイルも角丸ドット絵化

## 🏡 広場改善

- 住民の体を**白枠角丸ドット絵**へ（PeerIcon統一・鏡文字問題も構造的に解消）
- **新規住民フェードイン**を追加（新しい子がふわっと現れる・800ms・控えめ）
- おしゃべり吹き出し／向き変更／ぷるぷる待機／タップ→プロフィールは維持
- 広場レベルLv1〜6＋背景進化＋進捗バーは稼働中（実機でLv.1表示確認）
- 名簿タイルもレアリティ枠つき角丸ドット絵化

## 🔎 UX評価（A202SH実画面）

| 評価軸 | Before→After |
|---|---|
| 出会いを感じるか | ○→◎ 開封カードで相手の「作品」と対面する体験に |
| ドット絵が主役か | △→**◎** 全画面で最優先表示。自分の「ち」の字が広場中央・ヘッダー・ドックに |
| コミュニティ感 | ○ 住民＋吹き出し＋名簿の三層。相手が絵を描けば一気に街になる |
| 広場を眺めたくなるか | ○ 吹き出しが時々出るので「置いておく」動機が生まれた |
| アプリらしさ | ◎ プレースホルダー円が消え、レトロゲーム機の画面に近づいた |

残りのギャップ: 相手がまだ絵を描いていない場合はイニシャル（正しいフォールバック）。
**両者がv1.7.7以降で絵を描いてすれ違えば自動で相互表示**される。

## 🧪 テスト結果

- 回帰テスト **37件 全パス**（+3: toPiecePixels 16/32・peerPixels往復とreveal保持）
- flutter analyze: エラー0
- スモークQA（A202SH・v1.7.7照合✓）: 7タブ・クラッシュ0・エラー0
- スクショ差分: 新UIをベースラインとして更新
- 検出・修正: **BT OFF時にドット絵同期が走らない**（start()内のみだった）→ _loadDataから独立実行

## 📋 Git

- beta1.7.7+40: dot-art sync end-to-end / PeerIcon unification / kakera backfill /
  newcomer fade-in / BT-independent profile sync

## 👉 今後の推奨

1. **実世界E2E**: 2台とも今バージョンで絵を描き、BLEすれ違い→開門→相互ドット絵表示の実証
2. 広場「歩いて登場」演出（PLAZA_DESIGN.md §3・フェードインの次段階）
3. 開門・カケラ取得の効果音
4. ミニゲーム内の仲間アイコンもPeerIcon化（現在イニシャルサークルのまま）
5. Pixel端末のプロフィール再設定（現在オンボーディング状態のためQAベースライン未取得）
