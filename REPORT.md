# REPORT — Today/広場 大幅ブラッシュアップ（beta1.7.5+38）

作業日: 2026-07-04 / beta1.7.4 → **beta1.7.5+38**
検証環境: Android実機（A202SH）＋ Pixel 10 Emulator

---

## 📌 要約 — 何がどれだけ改善されたか

1. **広場が「リスト」から「生きた空間」になった** — 出会った人たちがドット絵の
   広場に立ち、ぷるぷる揺れ、ときどきひとことをしゃべる。人数で広場が
   Lv1「はじまりの広場」→Lv6「お祭り広場」へ発展。実機で夜の広場に提灯が灯り
   17人の住民が立つ様子を確認済み。
2. **Todayが「開門管理画面」から「出会いを祝福する場所」になった** — 人が主役。
   新しい出会いの瞬間に🎉ポップ演出、全員が待機アニメでゆれる。
3. **プライバシー強化** — 正確な時刻表示（HH:MM）を廃止し「あさ/ひるま/ゆうがた/よる
   すれ違ったよ」の曖昧表現へ（仕様の禁止事項対応）。
4. **APK更新競合の根本原因を特定し解決** — debug署名依存を廃止し専用リリース
   キーストアへ。実機で「アンインストールなし更新」の成功を実証。
5. **アプリアイコンを正式整備** — Adaptive Icon（全密度）を自動生成。

---

## 🎨 実装内容

### Todayタブ「今日の出会いを祝福する場所」
| 項目 | 内容 |
|---|---|
| 優先順位 | ①人（円弧カルーセル）②ひとこと吹き出し ③アイコン ④開門（最下段）— 仕様どおり |
| ぷるぷる待機 | 全員が位相をずらした正弦波でゆれる（1.8秒周期） |
| 出会い演出 | 人数増加を検知して「🎉 あたらしい出会い！」がelasticOutでポップ（2.5秒） |
| 時刻表現 | `🕐 21:34` → `🌙 よる すれ違ったよ`（1時間以内は`✨ さっき`） |
| 文言 | 「今日は N人と出会いました」→「きょうは N人 と出会えたよ 🎊」 |

### 広場タブ「今まで出会った人たちが集まる空間」
新規 `PlazaScene`（lib/ui/widgets/plaza_scene.dart）:
- **住民表示**: 最近の8人が前列4人/後列4人で広場に立つ。位置はpeerIdハッシュで
  決定論的（毎回同じ場所＝住んでいる感）。タップで詳細シート
- **ぷるぷる待機**: 位相ずらしの上下ゆれ／**向き変更**: ハッシュで左右向き
  （画像アバターのみ反転。イニシャル文字は鏡文字にしない）
- **おしゃべり演出**: 3.5〜6秒ごとにランダムな住民の上にひとこと吹き出しが
  2.6秒表示
- **じぶん**: 中央手前に自分のドット絵＋「じぶん」ラベル
- **広場レベル**: Lv1はじまりの広場(0)→Lv2ちいさな(5)→Lv3にぎやかな(15)→
  Lv4みんなの公園(50)→Lv5思い出の街(100)→Lv6お祭り広場(250)。
  次レベルへの進捗バー付き
- **背景発展**（CustomPaint）: 木(Lv2+で増加)→花(Lv3+)→ベンチ(Lv4+)→
  提灯6連(Lv6)。夜モードは星空＋提灯常時点灯

### 未実装分の設計案
- **新規住民出現演出**: 画面外から歩いてきて定位置に着く（Positioned+Tween、
  出現時にキラキラparticle）。resolve完了イベントをPlazaSceneへ流す配線が必要
- **カケラ発見演出**: 住民の頭上に💎が浮かび、タップでカケラタブへ飛ぶ導線
- **背景アセット案**（現在はCustomPaint。専用ドット絵に差し替える場合）:
  `assets/plaza/bg_lv1.png`〜`bg_lv6.png`（960×560, 昼/夜2種 or 昼のみ＋夜は
  シェーダー暗転）、`deco_tree.png / deco_bench.png / deco_lantern.png`
  （タイル配置用、各64×64）

---

## 🔧 APK更新問題 調査結果

**現象**: 一部端末で更新時に `INSTALL_FAILED_UPDATE_INCOMPATIBLE`（本セッションでも再現）

| 確認項目 | 結果 |
|---|---|
| signingConfig | **原因**。release が `signingConfigs.getByName("debug")` を使用。debug keystoreは `~/.android/debug.keystore` でマシン固有・再生成されうる → 署名変化で更新拒否 |
| applicationId | `com.example.ble_encounter` で一貫（問題なし。ただしストア公開前に要改名） |
| versionCode | pubspec `+N` から自動供給、単調増加で運用（問題なし） |
| release/debug差 | debug/releaseとも同じdebug署名だったため相互更新は可能だった。問題は「別マシン/keystore再生成」時 |

**対策（実装済み）**:
- `android/keystore/release.jks` を新規作成（RSA2048/10000日）
- `android/key.properties` から読み込む `release` signingConfig を build.gradle.kts に配線
  （key.properties が無い環境はdebug署名にフォールバック）
- 両ファイルとも **.gitignore 追加**（公開リポジトリのため）
- 実機で「uninstallなしの install -r 更新」成功を確認

⚠️ 注意: 署名切替のため**今回のみ**旧インストールのアンインストールが必要（実施済み）。
また `release.jks` は今後のアップデート署名の要。**バックアップ必須**
（紛失すると既存ユーザーへ更新配信不可）。

---

## 📱 アプリアイコン整理

- flutter_launcher_icons ^0.14.1 を導入し自動生成
  - **Adaptive Icon**: foreground=`assets/icons/tab_today.png`(inset16) /
    background=`#FBF3E4`（クリーム）→ mipmap-anydpi-v26 + 全密度PNG生成
  - **通常アイコン**: mipmap mdpi(48)〜xxxhdpi(192) 生成済み
- **通知アイコン**: 現状 `android.R.drawable.ic_menu_compass`（システム借用）。
  本来はモノクロ(白+透過)24dpの専用 `ic_stat_notify.png` を drawable-*dpi に
  用意すべき（残課題）
- **Play Store**: 512×512 PNG（フル正方形・透過不可）が別途必要。
  tab_today.png原寸から書き出し推奨。フィーチャーグラフィック1024×500も将来必要

---

## 🧪 テスト結果

- `flutter analyze`: lib配下エラー0
- release APK（56.2MB）ビルド成功（新署名）
- 実機A202SH: 広場シーン（夜モード・提灯・住民17人・Lv3・進捗バー・じぶんドット絵）
  表示確認 ✅ / Today（開門3アイコン・履歴）✅ / 起動時 匿名認証・トークン更新 ✅
- 更新インストール（install -r）成功 ✅
- 発見バグ修正: 住民の向き反転がイニシャル文字を鏡文字にしていた → 画像のみ反転に修正
- Emulator: OS側が Broken pipe でinstall不能に（アプリ起因ではない、要コールドブート）。
  検証は実機で完遂

---

## ⚠️ 残課題

1. 通知スモールアイコンが Android システムアイコン借用のまま
2. Play Store用 512×512 / フィーチャーグラフィック未作成
3. applicationId が com.example のまま（ストア公開前に要変更・変更すると別アプリ扱い）
4. 新規住民の「歩いて登場」演出・カケラ発見演出（設計案あり・未実装）
5. release.jks のバックアップ運用（パスワード管理含む）
6. エミュレータのadb不安定（環境問題）

## 💡 次回提案

1. resolve完了→PlazaScene への「新住民が歩いてくる」演出配線
2. 通知アイコンのドット絵化（モノクロ24dp）
3. 開門ファンファーレ・カケラ取得音などの効果音
4. 広場背景の専用ドット絵アセット化（bg_lv1〜6）
5. applicationId 改名＋署名を含むリリース手順書の整備
