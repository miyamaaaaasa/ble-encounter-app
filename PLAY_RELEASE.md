# PLAY_RELEASE.md — Google Play 公開手順

「はじめましてこんにちは」をGoogle Playで公開するための準備状況と、
Play Consoleで入力する内容をまとめたもの。

最終更新: 2026-09-16（beta1.9.1+48）

---

## 対応済み（コード側）

| 項目 | 内容 |
|---|---|
| パッケージ名 | `jp.hajimemashite.app`（`com.example` はPlayが拒否するため変更） |
| 署名 | `android/keystore/release.jks`（有効期限 2053年）でAAB署名済み |
| AAB | `build/app/outputs/bundle/release/app-release.aab`（53.3MB） |
| targetSdk | 36 |
| プライバシーポリシー | https://153-125-148-69.sslip.io/privacy/ （認証なしで公開） |
| データ削除機能 | 設定 → データ管理 → サーバーのデータを削除（実機で動作確認済み） |

### 削除した権限（審査リスク回避）

| 権限 | 削除理由 |
|---|---|
| `USE_EXACT_ALARM` | 時計・アラームアプリ専用の制限付き権限。このアプリでは使えない |
| `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` | 制限付き権限。かつコードで未使用だった |
| `READ_MEDIA_IMAGES` | 画像選択機能を廃止済みで未使用 |
| `READ_EXTERNAL_STORAGE` | 同上 |

### 位置情報権限の扱い

`ACCESS_FINE_LOCATION` / `ACCESS_COARSE_LOCATION` に `maxSdkVersion="30"` を付け、
**Android 12以降では要求しない**ようにした（`BLUETOOTH_SCAN` の
`neverForLocation` で足りるため）。Androidバージョンの判定は
`device_info_plus` で行い、11以下でのみ権限を要求する。

位置情報は取得も送信もしない（アプリの絶対維持事項）。

---

## Play Console で入力する内容

### アプリの内容 → データセーフティ

| 質問 | 回答 |
|---|---|
| データを収集または共有するか | **はい**（収集する。共有はしない） |
| データの暗号化 | **はい**（HTTPS） |
| データ削除のリクエスト方法 | **はい**（アプリ内で削除可能） |

収集するデータの申告:

| カテゴリ | データ種別 | 収集 | 共有 | 必須 | 目的 |
|---|---|---|---|---|---|
| 個人情報 | 名前 | はい | いいえ | 任意 | アプリの機能 |
| アプリのアクティビティ | その他のユーザー生成コンテンツ（ドット絵・自己紹介） | はい | いいえ | 任意 | アプリの機能 |
| アプリ情報とパフォーマンス | その他（最終アクセス日時） | はい | いいえ | 必須 | アプリの機能・分析 |

**「位置情報」は収集しないを選ぶ**（GPS不使用のため）。
広告ID・連絡先・写真・音声・財務情報はすべて「収集しない」。

### アプリの内容 → コンテンツのレーティング

- カテゴリ: **ソーシャル / コミュニケーション**（ゲーム要素はあるが交流が主目的）
- 想定される回答: 暴力なし・性的表現なし・不適切な言葉なし・ギャンブルなし
- **「ユーザー同士が交流できるか」→ はい**（表示名と定型の自己紹介が相手に見える）
- **「ユーザーの位置情報を共有するか」→ いいえ**

### アプリの内容 → 広告

- **広告は含まない**

### アプリのアクセス権

- ログイン不要。すべての機能が制限なく利用できる → **「特別なアクセス権は不要」**

### 対象年齢

- 13歳以上を推奨（ユーザー生成コンテンツがあるため）
- **「子ども向け」にはしない**（ファミリーポリシーの追加要件が発生するため）

---

## まだ必要なもの（あなたの作業）

### 1. Google Play デベロッパーアカウント

- 登録料 **$25（一回のみ）**
- 個人アカウントの場合、**登録から公開まで本人確認が必要**
- 個人開発者は**公開前に20人のテスターによる14日間のクローズドテストが必須**
  （2023年11月以降に作成したアカウントが対象。文化祭に間に合わせるなら最優先）

### 2. ストア掲載情報

| 素材 | 要件 | 状態 |
|---|---|---|
| アプリ名 | 30文字以内 | 済（11文字） |
| 簡単な説明 | 80文字以内 | 済（41文字） |
| 詳しい説明 | 4000文字以内 | 済（1314文字） |
| アプリアイコン | 512×512 PNG | 済 `store_assets/play_icon_512.png` |
| 機能グラフィック | 1024×500 PNG | 済 `store_assets/play_feature_1024x500.png` |
| スクリーンショット | スマホ用 最低2枚（推奨4〜8枚） | 済 5枚（`store_assets/shot_*.png`） |

ストア掲載文とアイコン・スクリーンショットは
[store_assets/](store_assets/) に用意済み（[store_listing.md](store_assets/store_listing.md) に
そのまま貼れる形で記載）。

| 項目 | 値 |
|---|---|
| 連絡先メールアドレス | hajimemashite.jp@na-cat.com |
| プライバシーポリシー | https://153-125-148-69.sslip.io/privacy/ |
| カテゴリ | アプリ / ソーシャル（ゲームタブが準備中のため） |

### 3. Play Console での操作

1. アプリを作成（アプリ名・デフォルト言語=日本語・アプリ/ゲームの選択）
2. ストアの設定・ストアの掲載情報を入力
3. **アプリの署名**: Play App Signing を有効化（推奨。鍵を紛失しても復旧できる）
4. AAB をアップロード
5. データセーフティ・コンテンツレーティング・対象年齢を入力
6. クローズドテスト → 製品版へ

---

## 注意点

### サーバーが止まるとアプリが機能しない

すれ違いの解決はサーバー経由のため、`153.125.148.69` が停止すると
新しい出会いが記録されなくなる。公開するなら、
**友人と共有しているサーバーである点**と、期限・支払いの管理に注意する。

### ドメインについて

現在は `sslip.io`（第三者運営の無料DNS）を使っている。
プライバシーポリシーのURLもここを指すため、**sslip.ioが停止するとポリシーが
参照できなくなり、Playの要件を満たさなくなる**。
長期運用するなら独自ドメインの取得を推奨（`server/README.md` に移行手順あり）。

### ゲームタブ

現在「近日公開」表示。ストアの説明文で「ゲーム機能は準備中」と書いておくと
ユーザーの期待とずれない。

---

関連: [server/README.md](server/README.md) / [CLAUDE.md](CLAUDE.md)
