# PROMPT_GUIDE.md — 有効ルール集約とプロンプトテンプレート

これまでの全セッションの指示を棚卸しし、**現在有効なルールだけ**を集約した。
今後のセッションではこのファイルを正とし、矛盾する古い指示は無効とする。

最終更新: 2026-07-04

---

## プロンプト大掃除の結果

### 検出した重複（→1本化）
| 重複していた指示 | 統合先 |
|---|---|
| 「絶対維持事項」が毎セッション微妙に違う表記で再掲 | 本書§1に一本化 |
| 「バージョンをインクリメント」「beta維持」が3セッションで別表現 | §5 Git運用 |
| 「commit/push を意味単位で」重複 | §5 |
| REPORT.md作成指示が毎回内容項目違いで再定義 | §6 テンプレB |

### 検出した矛盾・古い方針（→無効化）
| 古い指示 | 状態 | 現在の正 |
|---|---|---|
| BLEペイロードに名前/趣味/都道府県を載せる(〜v1.6.4) | **無効** | サーバーファースト。BLEはトークンのみ |
| 画像アバター（image_picker+Storage） | **無効** | ドット絵アイコン一本化（画像コードは残存するが新規利用禁止） |
| 「Today画面に取得時刻を表示」(v1.7.2指示) | **上書き** | 正確時刻は禁止→曖昧表現(v1.7.5指示が優先) |
| release署名=debugキーストア | **無効** | android/keystore/release.jks + key.properties |
| 起動時に電波解析ボタン必須 | **無効** | 3分間隔の自動解析 |
| バージョン表記「beta1.5.x」系列 | 済 | 現行 beta1.7.x（betaプレフィクスのみ不変） |

### 不要指示（今後書かなくてよいもの）
- 「既存実装を理解してから修正して」→ §0 恒久ルール化済み
- 「ログ確認→分析→修正→ビルド→テスト」→ WORKFLOW.md の標準サイクル化済み

---

## §0. 恒久ルール（毎セッション有効・再掲不要）

1. 変更前に既存実装の意図を読む。過去の修正（0時/18時バグ・カウント爆増・
   BLE安定化・R8対策・永続化群）を破壊しない
2. 1サイクル = 分析→実装→analyze→build→QA→デプロイ→commit→push
3. クラッシュ修正 > データ消失防止 > 永続化 > BLE安定 > UX > UI > コード品質
4. 作業完了時に REPORT.md を更新する

## §1. コンセプトと絶対維持事項

**「Bluetoothですれ違った人との出会いを楽しむコミュニティゲーム」**
（SNSではない・BLEツールでもない）

破壊禁止: GPS不使用 / BLE中心 / 匿名性 / 気配演出 / 開門システム(9・12・21時) /
回転トークン方式 / プライバシー保護（数値の抽象化・正確な時刻や場所を出さない・
裏でのアドバタイズ停止）

禁止表現: 位置情報的表現・個人特定につながる情報・正確なすれ違い時刻(HH:MM)

## §2. UI方針

- **Material脱却**: TopAppBar / NavigationBar / FAB / Material Card / Material Dialog 禁止
  （例外: 設定・プロフィールのフォーム部品は当面許容）
- 独自UIキットを使う: `SoftPanel` `ChunkyButton` `RoundIconButton` `ScreenHeader`
  `GameDock` `SpeechBubble` `StatChip` `SectionLabel` `CandyProgress`（lib/ui/widgets/ui_kit.dart）
- 配色は `Palette`（lib/ui/theme/palette.dart）のみ。**ハードコード色は原則禁止**
  （例外: カケラ夜空世界 nightSky系 とゲーム内部）
- ライト=「昼の広場」クリーム×パステル / ダーク=「夜の広場」藍×提灯色。
  `Palette.night` に追従させること（const TextStyleにPalette色を入れない）
- 影は Palette.lift()/liftBig()（ぼかさないハードシャドウ）
- アイコンは assets/icons・assets/gate の正式ピクセルアイコン。絵文字アイコン禁止
  （文章内の絵文字装飾は可）
- ユーザーアイコンは `UserIcon`（ドット絵優先→デフォルトニコちゃん）

## §3. UX方針

- ひらがな多めの温かい文体（「じぶん」「カケラのよぞら」「〜だよ」）
- 出会いは祝福する（🎉演出）、待機はぷるぷる（正弦波ゆれ）、押すと沈む(_Pressable)
- 数値の生表示を避ける（再遭遇回数→抽象ラベル、時刻→時間帯表現）
- 空状態は寂しくしない（望遠鏡「だれか来ないかな…」等、次の行動を示す）
- 開門前の相手情報は絶対に見せない（気配のみ）

## §4. BLE方針

- ペイロード: `[0xBE][token16B]` のみ。プロフィール情報を電波に載せない
- トークンは TokenService（24h・残2hで更新・SecureStorage）
- スキャンサイクルは時計同期（ScanInterval）。並列起動ガード(_cycleOnActive)を壊さない
- 未解析トークン(PendingScanStorage)は**時間経過で消さない**（解析成功時のみ削除）
- resolveTokens は 失敗=null / 成功空=[] を区別（nullでトークン保持）
- FGS起動は try-catch 必須（BLE非対応端末対策）

## §5. Git運用・バージョン・命名規則

- バージョン: `pubspec.yaml` の `X.Y.Z+N`。1改善サイクルごとに Z と N を+1。
  表示は `beta` プレフィクス維持（settings画面が 'beta$version' を組み立てる）
- コミット: 意味のある改善単位ごと。メッセージは
  `betaX.Y.Z: <要約英語>` + 箇条書き詳細 + `Co-Authored-By: Claude`
  **必ず `git commit -F <file>` を使う**（PowerShell here-string事故防止）
- push は commit 直後に毎回
- 署名: android/key.properties + keystore/release.jks（gitignore対象・要バックアップ）
- 命名: ファイル=snake_case.dart / クラス=UpperCamel / プライベートWidget=_Prefix /
  SharedPreferencesキー=`name_v1`形式 / アセット=`tab_*.png` `gate_*.png`

## §6. 再利用テンプレート

### テンプレA: 機能追加/改修 依頼（人間→AI）
```
# <機能名>
目的: <1行。どの体験価値(出会い/収集/ワクワク等)を上げるか>
対象画面: <タブ名/画面名>
要件:
- <箇条書き>
禁止: <あれば>
完了条件: <目に見える確認方法>
※絶対維持事項・PROMPT_GUIDE.mdは既定で適用
```

### テンプレB: セッション完了報告（AI→人間）
```
## 完了報告 betaX.Y.Z+N
- 実施: <箇条書き>
- 修正バグ: <現象→原因→対処>
- 検証: analyze / build / 実機 or エミュ巡回 / (BLE E2E)
- コミット: <hash 要約>
- お願いリスト: <人間にしかできない事項（SQL実行等）>
- 残課題/次回候補: <箇条書き>
```

### テンプレC: バグ報告（人間→AI）
```
# バグ: <現象1行>
再現: <操作手順 or 「不明」>
端末: <実機/エミュ/両方>
期待: <本来の挙動>
```

### テンプレD: 無人QA依頼
```
tool/qa_smoke.sh を実行し、QA_GUIDE.mdのチェックリストで
全タブのスクショを評価。問題があれば修正→再QA→commit/pushまで。
```
